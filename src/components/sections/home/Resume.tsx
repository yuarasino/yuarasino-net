import { defineComponent } from "~/utils/preact.ts";

export const Resume = defineComponent(() => {
  return (
    <section class="min-h-screen pt-16" id="resume">
      <div class="max-w-screen-clamp mx-auto p-4">
        <h2 class="uppercase">resume</h2>
      </div>
    </section>
  );
});
