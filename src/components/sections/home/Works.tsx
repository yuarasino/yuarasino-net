import { defineComponent } from "~/utils/preact.ts";

export const Works = defineComponent(() => {
  return (
    <section class="min-h-screen pt-16" id="works">
      <div class="max-w-screen-clamp mx-auto p-4">
        <h2 class="uppercase">works</h2>
      </div>
    </section>
  );
});
